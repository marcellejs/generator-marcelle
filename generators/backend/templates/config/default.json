{
  "host": "localhost",
  "port": 3030,
  "public": "../public/",
  "database": "<%= db %>",
  "services": ["instances"],
  "enableAuth": <%= auth %>,
  "nedb": "../data",
  "mongodb": "mongodb://localhost:27017/marcelle_backend",
  "paginate": {
    "default": 10,
    "max": 50
  },
  "authentication": {
    "entity": "user",
    "service": "users",
    "secret": "<%= secret %>",
    "authStrategies": [
      "jwt",
      "local"
    ],
    "jwtOptions": {
      "header": {
        "typ": "access"
      },
      "audience": "https://yourdomain.com",
      "issuer": "feathers",
      "algorithm": "HS256",
      "expiresIn": "1d"
    },
    "local": {
      "usernameField": "email",
      "passwordField": "password"
    }
  }
}